import React from 'react';
import PetInfo from '../PetProfile/PetInfo/PetInfo';
import Cards from '../PetProfile/Cards/Cards';

const PetProfileUser = () => {
  return (
    <div>
      <PetInfo />
      <Cards />
    </div>
  );
};

export default PetProfileUser;
