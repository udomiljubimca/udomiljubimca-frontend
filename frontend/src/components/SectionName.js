import React from "react";
// Section Name - Orange letters with cross line on the left
const SectionName = ({text}) => {
    return (
        <h2 className="page-title mt-4">{text}</h2>
    )
}

export default SectionName;